# 环境准备

## 1. 安装 Python 与 MkDocs

```powershell
pip install mkdocs mkdocs-material
```

## 2. 本地预览

在 `Notes` 目录执行：

```powershell
mkdocs serve
```

访问 `http://127.0.0.1:8000`。

## 3. 新增页面

1. 在 `docs/` 新建 `.md` 文件
2. 在 `mkdocs.yml` 的 `nav` 里添加对应条目

